{erl_opts, [
    debug_info
]}.

{minimum_otp_vsn, "27"}.

{deps, [
]}.

{shell, [
    {config, "config/shell.config"},
    {apps, [ro2erl_bridge, runtime_tools]}
]}.

{profiles, [
    {test, [
        {deps, []},
        {eunit_opts, [verbose]}
    ]}
]}.
